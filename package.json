{
  "name": "dokku-issue-repro-killing-server",
  "version": "1.0.0",
  "description": "Server that has two endpoints, a normal endpoint and one that crashes the server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.4"
  }
}
